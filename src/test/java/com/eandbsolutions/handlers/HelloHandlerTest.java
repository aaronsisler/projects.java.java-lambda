package com.eandbsolutions.handlers;

import static org.junit.Assert.assertTrue;

//@RunWith(MockitoJUnitRunner.class)
public class HelloHandlerTest {
    //    @Test
    public void shouldAnswerWithTrue() {
        assertTrue(true);
    }

    //    @Test
    public void onSuccessShouldReturnCorrectMessage() {
//        Context context = mock(Context.class);
//        String result = new HelloHandler().handleRequest(null, context);

//        assertEquals(result, "{\"body\":\"Hello World!\",\"statusCode\":200}");
    }

//     @Test
//     public void onFailureShouldReturnCorrectMessage() throws Exception {
//         // Given
//         Context context = mock(Context.class);
//         HelloHandler helloHandler = new HelloHandler();
// //        when(context.getClientContext()).thenThrow(new Exception("Expected test exception"));

// //        doThrow(new Exception("Expected test exception")).when(context.getClientContext());
//         doThrow(new IllegalArgumentException("Expected test exception")).when(context).getClientContext();

//         // When
//         String result = helloHandler.handleRequest(null, context);

//         // Then
//         assertEquals(result, "Lambda broke");
// //        verify(context.getLogger(), times(1)).isWatchedBranch(any());
//     }
}

